runtime: nodejs18
service: user-darurat

env_variables:
  DB_HOST:
  DB_PORT:
  DB_NAME:
  DB_USER:
  DB_PASS:
  API_KEY:
  APP_PORT:

handlers:
  - url: /.*
    script: auto
    secure: always
    redirect_http_response_code: 301
